CREATE TABLE SCHOOLS(
    ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1, INCREMENT BY 1) PRIMARY KEY,
    NAME VARCHAR(255)
);
--
INSERT INTO SCHOOLS (ID, NAME) VALUES (1, 'English Forward');
--
INSERT INTO SCHOOLS (NAME) VALUES ('English Forward');
--
CREATE TABLE BRANCHES(
    ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1, INCREMENT BY 1) PRIMARY KEY,
    NAME VARCHAR(255),
    SCHOOL_ID INT,
    FOREIGN KEY (SCHOOL_ID) REFERENCES SCHOOLS(ID)
);
--
INSERT INTO BRANCHES (ID, NAME, SCHOOL_ID)
    VALUES (1, 'EF - Горьковская', 1),
           (2, 'EF - Парк Победы', 1);
--
INSERT INTO BRANCHES (NAME, SCHOOL_ID)
VALUES ('EF - Горьковская', 1),
       ('EF - Парк Победы', 1);
--
CREATE TABLE ROLES(
    ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1, INCREMENT BY 1) PRIMARY KEY,
    ROLE VARCHAR(30) NOT NULL
);
--
INSERT INTO ROLES (ROLE) VALUES ('ADMIN'), ('TEACHER'), ('STUDENT'), ('UNKNOWN');
--
CREATE TABLE USERS(
    ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1, INCREMENT BY 1) PRIMARY KEY,
    LASTNAME VARCHAR(255) NOT NULL,
    FIRSTNAME VARCHAR(255) NOT NULL,
    MIDDLENAME VARCHAR(255),
    EMAIL VARCHAR(255) NOT NULL,
    LOGIN VARCHAR(255) NOT NULL,
    PASSWORD VARCHAR(255) NOT NULL,
    ROLE_ID INT NOT NULL,
    BRANCH_ID INT NOT NULL,
    FOREIGN KEY (ROLE_ID) REFERENCES ROLES(ID),
    FOREIGN KEY (BRANCH_ID) REFERENCES BRANCHES(ID)
);
--
INSERT INTO USERS (LASTNAME, FIRSTNAME, MIDDLENAME, EMAIL, LOGIN, PASSWORD, ROLE_ID, BRANCH_ID) VALUES
    ('Ruzaeva', 'Alisa', 'Andreevna', 'alise.ruzaeva@yandex.ru', 'alise.ruzaeva@yandex.ru', '1', 3, 2);